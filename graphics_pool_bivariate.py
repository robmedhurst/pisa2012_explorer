"""
Placeholder functions for looking up bivariate plots.

Functions for bivariate group plotting with verbose names to be
caught by keyword.
"""

import graphics_ as gs


def violinplot_bi_row_cat(response_info, user_data):
    """."""
    return gs.violinplot_bi_row_cat(response_info, user_data)


def boxplot_bi_row_cat(response_info, user_data):
    """."""
    return gs.boxplot_bi_row_cat(response_info, user_data)


def countplot_bi_row_cat(response_info, user_data):
    """."""
    return gs.countplot_bi_row_cat(response_info, user_data)
